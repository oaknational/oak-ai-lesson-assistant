{
  "name": "oak-ai-web-app",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "FIXME:lint": "turbo lint && manypkg check",
    "build": "turbo build",
    "build:dev": "pnpm with-env turbo build:dev",
    "check": "turbo type-check",
    "clean": "rm -rf node_modules",
    "clean:workspaces": "turbo clean",
    "db-export:dev": "doppler run --config dev -- bash scripts/db_export.sh",
    "db-generate": "turbo db-generate",
    "db-generate:no-engine": "turbo db-generate:no-engine",
    "db-migrate": "turbo db-migrate",
    "db-publish:stg": "doppler run --config stg -- bash scripts/db_publish.sh",
    "db-push": "turbo db-push",
    "db-push-force": "turbo db-push-force",
    "db-seed": "turbo db-seed",
    "dev": "FORCE_COLOR=1 turbo dev --parallel --ui=stream --log-prefix=none --filter=!@oakai/db",
    "dev:react-scan": "NEXT_PUBLIC_ENABLE_RENDER_SCAN=true pnpm dev",
    "dev:react-scan-monitor-cloud": "NEXT_PUBLIC_RENDER_MONITOR=true pnpm dev",
    "doppler:pull:dev": "doppler secrets download --config dev --no-file --format env > .env",
    "doppler:pull:stg": "doppler secrets download --config stg --no-file --format env > .env",
    "doppler:run:stg": "doppler run -c stg --silent",
    "lint": "turbo lint",
    "lint:debug": "pnpm lint -- --debug",
    "lint:file": "eslint",
    "lint:fix": "turbo lint:fix",
    "node-version": "node -v",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "prompts": "turbo prompts",
    "prompts:dev": "turbo prompts:dev",
    "sonar": "dotenv -e ./.env -- sonar-scanner",
    "storybook": "turbo storybook",
    "test": "turbo test -- --maxWorkers=33%",
    "test-coverage": "COLLECT_TEST_COVERAGE=true turbo test-coverage -- --maxWorkers=33%",
    "test-e2e": "turbo test-e2e --",
    "test-e2e-ui": "turbo test-e2e-ui --",
    "test:seq": "turbo test:seq --concurrency=1 --",
    "type-check": "turbo type-check",
    "with-env": "dotenv -e ../../.env --"
  },
  "prettier": "@oakai/prettier-config",
  "eslintConfig": {
    "extends": "@oakai/eslint-config"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@ai-sdk/openai": "^0.0.72",
    "@cspell/dict-en-gb": "^5.0.14",
    "@manypkg/cli": "^0.25.1",
    "@next/env": "14.2.18",
    "@oakai/eslint-config": "*",
    "@oakai/prettier-config": "*",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@types/jest": "^30.0.0",
    "autoprefixer": "^10.4.21",
    "cursor-tools": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.1",
    "next": "16.0.10",
    "postcss": "^8.5.6",
    "semantic-release": "^24.2.9",
    "serialize-error": "^12.0.0",
    "sonarqube-scanner": "^4.3.2",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@8.6.12",
  "engines": {
    "node": ">=22.18.0",
    "pnpm": ">=8"
  },
  "pnpm": {
    "overrides": {
      "@types/estree@1.0.x": "1.0.6",
      "body-parser@2.2.0": ">=2.2.1",
      "cross-spawn@>=7.0.0 <7.0.5": ">=7.0.5 <7.1.0"
    }
  },
  "dependencies": {
    "@oaknational/oak-components": "^2.2.0"
  }
}
