name: Deployment Actions

on: deployment_status

jobs:
  dump_github_event:
    uses: ./.github/workflows/dump_event.yml

  # Do a find-and-replace to add a link to the deployment to the PR body
  update_pr_description:
    # Only want to run on success, otherwise it might be "pending", or "failure".
    # Filter out storybook deployments and temporarily Netlify deployments
    if: ${{ (github.event.deployment.ref != 'main') && (github.event.deployment_status.state == 'success') && !startsWith(github.event.deployment_status.environment, 'storybook')}}
    name: Add deploy URL to PR description
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install deps (with cache)
        run: pnpm install

      - uses: ./.github/actions/ref_from_sha
        name: Get PR Ref from SHA
        id: ref_from_sha
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update deploy URL in PR description
        if: ${{ steps.ref_from_sha.outputs.pr_number }}
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = parseInt('${{ steps.ref_from_sha.outputs.pr_number }}');
            const deployUrl = '${{ github.event.deployment_status.environment_url }}';

            core.info(`Updating PR #${prNumber} with deploy URL: ${deployUrl}`);

            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });

            let body = pr.body || '';

            // Replace content between sentinel comments — works on every deployment, not just the first
            let updated = body.replace(
              /<!-- vercel-preview -->.*?<!-- \/vercel-preview -->/gs,
              `<!-- vercel-preview -->${deployUrl}<!-- /vercel-preview -->`
            );

            // Fall back to plain placeholder (PR opened before sentinels landed on main).
            // Inject sentinels during replacement so subsequent deployments can update it too.
            if (updated === body) {
              core.info('Sentinels not found, falling back to plain {deployment_url} placeholder');
              updated = body.replace(
                '{deployment_url}',
                `<!-- vercel-preview -->${deployUrl}<!-- /vercel-preview -->`
              );
            }

            if (updated === body) {
              core.info('No placeholder or sentinels found in PR body — nothing to update');
              return;
            }

            core.info('Updating PR description...');
            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
              body: updated,
            });
            core.info('PR description updated successfully');
